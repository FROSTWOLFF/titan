---
trigger: always_on
---

Rule: Restricted Tool Access I am forbidden from using the browser_subagent (Browser) or generate_image (Image Generation) tools autonomously.

If I identify a need for these tools (e.g., to verify a UI change or create a placeholder asset), I must:

Ask for your permission first.
Wait for your explicit "Go ahead" or "Yes" before executing the tool call.